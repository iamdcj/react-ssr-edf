import styled from "@emotion/styled";

const FeatureContainer = styled.section``;

const FeatureInner = styled.div``;

const FeatureTitle = styled.h2``;

export { FeatureContainer, FeatureInner, FeatureTitle };
