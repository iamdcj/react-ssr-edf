import styled from "styled-components";

const QuoteContainer = styled.blockquote``;

const QuoteInner = styled.div``;

const QuoteTitle = styled.h2``;

export { QuoteContainer, QuoteInner, QuoteTitle };
