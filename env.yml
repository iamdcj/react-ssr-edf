default_env: &default_env
  NODE_PATH: src ## for jest

local:
  NODE_ENV: development
  ENV: development
  PORT: 3000
  API_BASE: localhost
  <<: *default_env

development:
  NODE_ENV: development
  ENV: development
  PORT: 3000
  API_BASE: localhost
  <<: *default_env

staging:
  NODE_ENV: staging
  ENV: staging
  API_BASE: 3000
  <<: *default_env

production:
  NODE_ENV: production
  ENV: production
  PORT: 3000
  API_BASE: localhost
  <<: *default_env
